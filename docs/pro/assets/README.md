# Pro design assets (private)

Canonical visual references for the Pro subsystems. Everything here is **Pro-only**
and private — never mirrored to the public free-core repo.

## Floating Assistant

- **`floating_assistant_mockup.png`** — the approved reference mockup for the
  Genesis Floating Assistant (bubble + expanded panel + Canvas mini + capability
  strip + activity slider + onboarding). Owner-provided.
  - _Action:_ drop the PNG here (the owner has the file); the spec
    (`../FLOATING_ASSISTANT_SPEC.md` → "Visual reference") already describes it in
    text so implementation is not blocked if the image is added later.

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "genesis-architect-pro"
version = "6.0.0"
description = "Genesis Architect Pro - architecture analysis, anti-pattern detection, security hardening, C4 diagrams, and deep research intelligence. Requires a license."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "genesis-architect>=5.4.0",
    "cryptography>=42",
]

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.4", "pytest-cov>=5"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py311"
